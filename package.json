{
  "name": "unit-tests-jest",
  "version": "1.0.0",
  "description": "Prova 01",
  "main": "index.js",
  "author": "nicolas",
  "license": "ISC",
  "dependencies": {
    "jest": "^30.0.5",
    "rimraf": "^6.0.1",
    "mkdirp": "^3.0.1"
  },
  "scripts": {
    "clean": "rimraf ./coverage && mkdirp ./coverage",
    "test": "npm run clean && jest",
    "coverage": "npm run clean && jest --coverage"
  },
  "jest": {
    "coverageReporters": [
      "lcov",
      "text"
    ]
  }
}
